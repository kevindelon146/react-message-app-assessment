import { HashLoader } from "react-spinners";

const LoadingPage = () => (
  <div className="h-screen flex flex-col justify-center items-center">
    <HashLoader color="#3b82f6" size={65} />
  </div>
);

export default LoadingPage;
